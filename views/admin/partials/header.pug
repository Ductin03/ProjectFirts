//-THÀNH PHẦN HEADER
header.header
    .container-fluid
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href=`${fixadmin}/dashboard`) ADMIN

            .col-9
                div(class="text-right")
                    a(
                        href=`${fixadmin}/auth/logout`
                        class="btn btn-danger btn-sm"
                    ) Đăng xuất 
            