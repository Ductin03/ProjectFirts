//-TẠO GIAO DIỆN TÌM THEO TÊN SẢN PHẨM(REGEX)
mixin search(Keyword)
    form(id="form-search")
                        .input-group
                            input(
                                type="text"
                                placeholder="Nhập từ khóa"
                                name="keyword"
                                class="form-control"
                                value=Keyword
                            )
                            .input-group-append
                            button(
                                class="btn btn-success"
                                type="submit"
                            ) Tìm